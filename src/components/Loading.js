import React from 'react'

class Loading extends React.Component {
    render() {
        return <h1>Loading should only be rendered when the loading state is true</h1>
    }
}

export default Loading